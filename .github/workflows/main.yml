# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      #- name: Trigger Jenkins Job
        # You may pin to the exact commit or the version.
        # uses: appleboy/jenkins-action@b9936d50c66383ffb58f3504c6e1348d94425c59
      #  uses: appleboy/jenkins-action@0.0.1
      #  with:
      #    url: "https://75d6e155db47.ngrok.io"
      #    user: "admin"
      #    token: "1188ec2731122a05cc805b9c17f5f5fdef"
      #    job: "PipelineArtifactsDemo"
          
      - name: Build Jenkins job
        # You may pin to the exact commit or the version.
        # uses: GoldenspearLLC/build-jenkins-job@ba59c17c14a8883af2d7f9a7c1153c1284f7c1c6
        uses: GoldenspearLLC/build-jenkins-job@1.0.0
        with:
          # jenkins server url
          jenkins-url: "https://75d6e155db47.ngrok.io"
          jenkins-token: "1188ec2731122a05cc805b9c17f5f5fdef"
          user: "admin"
          # job/folder_name/job/job_name
          job-path: "PipelineArtifactsDemo"

